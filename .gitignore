# ─────────────────────────────────────────────
# Node / TypeScript (library)
# ─────────────────────────────────────────────
node_modules/
dist/
build/
lib/
.eslintcache
.stylelintcache
.npmrc
*.tsbuildinfo

# Test & coverage
coverage/
.nyc_output/
coverage-final.json
junit.xml

# Package managers
# (Keep lockfiles committed for libs; comment these out if you prefer to ignore)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Tool caches
.parcel-cache/
.rollup.cache/
.rpt2_cache/
.turbo/
.cache/
.vite/
.next/
.storybook-out/
vite.config.ts.timestamp-*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env (do not commit secrets)
.env
.env.*.local
.env.local

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo

# OS cruft
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*~

# ─────────────────────────────────────────────
# Optional: if you add an example app inside /example
# ─────────────────────────────────────────────
example/node_modules/
example/dist/
example/android/.gradle/
example/android/build/
example/android/app/build/
example/android/local.properties
example/android/app/release/
example/android/app/debug/
example/ios/build/
example/ios/DerivedData/
example/ios/Pods/
example/ios/*.xcodeproj/project.xcworkspace/xcuserdata/
example/ios/*.xcworkspace/xcuserdata/
example/ios/*.xcodeproj/xcuserdata/
example/ios/*.xcodeproj/project.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
example/ios/*.xcodeproj/xcshareddata/xcschemes/*
example/ios/*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
example/ios/.swiftpm/
example/ios/*.pbxuser
example/ios/*.mode1v3
example/ios/*.mode2v3
example/ios/*.perspectivev3
example/ios/*.moved-aside
example/ios/.DS_Store

# Android keystores (never commit real keys)
example/android/keystores/
*.keystore
*.jks

# Expo (if you test with it in example)
example/.expo/
example/.expo-shared/

# Metro / React Native cache
example/.metro-cache/
.metro-cache/
